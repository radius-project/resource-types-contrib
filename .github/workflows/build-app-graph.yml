# This workflow is auto-generated by Radius to build the application graph.
name: Build Application Graph

on:
  push:
    branches: [main]
    paths: [.radius/app.bicep]
  pull_request:
    paths: [.radius/app.bicep]

permissions:
  contents: write

jobs:
  build-graph:
    uses: radius-project/radius/.github/workflows/__build-app-graph.yml@features/radius-graph
    with:
      app_file: .radius/app.bicep
      orphan_branch: radius-graph
      workflow_source_ref: features/radius-graph
